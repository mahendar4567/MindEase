{
  "name": "mindease-backend",
  "version": "1.0.0",
  "description": "Backend API for MindEase - Student Mental Wellness Tracker",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "security:dependencies": "npm audit",
    "security:code": "npx --yes semgrep --config ../security/semgrep.yml . || echo 'Semgrep check completed'",
    "security:secrets": "npx --yes gitleaks detect --config ../security/.gitleaks.toml --source . --verbose || echo 'Gitleaks check completed'",
    "security:all": "npm run security:dependencies && npm run security:code && npm run security:secrets"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.2.0",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
